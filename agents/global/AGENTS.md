The gh command requires internet access.
Check whether a development server is already running before starting a new one.
Always keep documentation aligned with current implementation. Do not put plans into docs. 